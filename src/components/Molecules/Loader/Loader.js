import Loading from 'components/Atoms/Loading';
import { StyleWrapper } from './style';

const Loader = () => {
  return (
    <StyleWrapper>
      <Loading />
    </StyleWrapper>
  );
};

export default Loader;
