import LoadingFilled from 'components/Atoms/Icons/LoadingFilled';
import { StyleLoading } from './style';

const Loading = () => {
  return (
    <StyleLoading>
      <LoadingFilled />
    </StyleLoading>
  );
};

export default Loading;
